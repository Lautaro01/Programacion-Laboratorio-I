#include <stdio.h>
#include <stdlib.h>

int main()
{
    aux = (int*) realloc(vec, (TAM + 5) * sizeof(int))

    if(aux == NULL)
    {
        no habi alugar hlahlahla
    }
    else
    {
        veo = aux;

    for(int i=0; i<(TAM + 5);i++)
    {
        printf("Ingrese un un numero: ");
        scanf("%d", vec+i);
    }
    }
    return 0;
}
